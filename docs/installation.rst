============
Installation
============

At the command line::

    $ easy_install bushmaster

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv bushmaster
    $ pip install bushmaster
